{
    "navigationFallback": {
      "rewrite": "/index.html",
      "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
    },
    "routes": [
        {
            "route": "/api/*",
            "rewrite": "https://mern-deployement.onrender.com/api/*"
        }
    ]
}
